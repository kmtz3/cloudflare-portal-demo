{
  "name": "cloudflare-portal-demo",
  "version": "1.0.0",
  "description": "LumApps Product Roadmap portal via Cloudflare Pages",
  "main": "index.js",
  "scripts": {
    "dev": "wrangler pages dev . --port 8787",
    "deploy": "wrangler pages deploy .",
    "build": "mkdir -p dist && cp index.html dist/ && cp -r functions dist/"
  },
  "keywords": ["cloudflare", "pages", "portal"],
  "author": "",
  "license": "MIT"
}
